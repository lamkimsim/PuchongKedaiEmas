import React from "react";
import { Typography } from "antd";

const HomeScreen = () => {
  const { Title } = Typography;
  return <Title level={3}>Latest Product</Title>;
};

export default HomeScreen;
